// Stub for vue-slider-component: its shipped Vue-3 *beta* types fail to
// type-check (a .vue in the package errors under vue-tsc). Redirected here via
// tsconfig `paths`. Typed as `any` so its named slots aren't checked. Slated
// for replacement in the UX rework (Phase 4).
declare const VueSlider: any
export default VueSlider
