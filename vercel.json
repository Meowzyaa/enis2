{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "yarn install --frozen-lockfile",
  "buildCommand": "yarn workspace web-mars build",
  "outputDirectory": "apps/web-mars/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
